<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.isolutio.gestionaula"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="es.isolutio.gestionaula.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="es.isolutio.gestionaula.CursoActivity"
            android:label="@string/title_activity_curso" >
        </activity>
        <activity
            android:name="es.isolutio.gestionaula.AlumnoActivity"
            android:label="@string/title_activity_alumno" >
        </activity>
    </application>

</manifest>
